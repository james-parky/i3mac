[package]
name = "core_graphics"
version = "0.1.1"
edition = "2024"

[dependencies]
core_foundation = { path = "../core_foundation" }
libc = { workspace = true }
serde = { version = "1.0.228", features = ["derive"] }
